#include <stdio.h>
#include <stdlib.h>
#define MAX_SIZE 100
typedef int ElemType;
typedef struct sqlist
{
    ElemType data[MAX_SIZE];
    int length;
} Sqlist;
int main(int argc, char const *argv[])
{

        /* code */
    return 0;
}
