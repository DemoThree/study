# VS Code C/C++ 环境问题排查与修复总结

## 环境信息

- **操作系统**: Windows
- **编译器**: MinGW g++ 8.1.0 (`C:\Program Files\mingw64\bin\g++.exe`)
- **项目路径**: `c:\Users\wangte\.vscode\todos\cStudy\study-1`

---


## 问题 2：Shell 类型任务 — PowerShell 路径引号截断

### 现象
```
g++.exe: error: \output\linklist.exe: No such file or directory
& : 无法将"c:\Users\wangte\...\study-1\list\"项识别为 cmdlet...
```

### 原因
`tasks.json` 中 `"g++ build active file"` 任务类型为 `shell`。VS Code 将 `command` + `args` 拼接成命令行字符串时，Windows 路径中的反斜杠 `\` 与双引号 `"` 交互，PowerShell 把 `\"` 误解析为转义引号，导致路径被截断。

例如拼接后的命令：
```
& "c:\...\study-1\list\"\output\linklist.exe
```
`list\` 后的 `"` 被当作字符串结束，`\output\linklist.exe` 被当作独立 token。

### 修复
将默认构建任务改为 `cppbuild` 类型（不经过 shell 解析）：

```jsonc
// tasks.json
{
    "type": "cppbuild",                    // 关键：用 cppbuild 而非 shell
    "label": "C/C++: g++.exe build active file",
    "group": { "kind": "build", "isDefault": true }
}
```

---

## 问题 3：Code Runner 右键运行 — 同样路径引号问题

### 现象
右键 Run Code 同样报路径错误。

### 原因
`settings.json` 中 `code-runner.executorMap` 使用了 `$dir`（带尾部 `\`）拼接路径：
```json
"c": "cd $dir ; & 'g++.exe' $fileName -o $dir\\output\\$fileNameWithoutExt.exe ..."
```
`$dir` = `c:\...\list\`，拼接 `\\output\\` 后，PowerShell 解析时 `\"` 导致路径截断。

### 修复
先 `cd $dir` 切到文件目录，后续全部使用**相对路径**，彻底避开绝对路径拼接问题：
```json
"c": "cd $dir ; taskkill /f /im $fileNameWithoutExt.exe 2>$null ; & 'C:\\Program Files\\mingw64\\bin\\g++.exe' $fileName -o output\\$fileNameWithoutExt.exe -std=c++17 ; & .\\output\\$fileNameWithoutExt.exe"
```

---

## 问题 4：Permission denied — 进程残留

### 现象
```
cannot open output file output\linklist.exe: Permission denied
```

### 原因
程序使用 `scanf()` 等待键盘输入，运行后进程一直卡住不退出。再次编译时 `.exe` 文件被占用，无法覆盖写入。

### 修复
在 Code Runner 编译命令前加上 `taskkill` 强制终止残留进程：
```
taskkill /f /im $fileNameWithoutExt.exe 2>$null ;
```
`2>$null` 用于抑制"进程不存在"时的错误提示。

---

## 问题 5：F5 调试无法编译

### 现象
按 F5 调试时编译失败。

### 原因
`launch.json` 中 `preLaunchTask` 指向的是有问题的 shell 任务：
```json
"preLaunchTask": "g++ build active file"   // shell 类型，有引号 bug
```

### 修复
改为指向 cppbuild 任务：
```json
"preLaunchTask": "C/C++: g++.exe build active file"
```

---

## 关键配置文件最终状态

### `.vscode/tasks.json`
- 默认构建任务：`C/C++: g++.exe build active file`（**cppbuild** 类型）
- 附带 `-std=c++17` 编译参数

### `.vscode/settings.json`
```json
{
    "code-runner.executorMap": {
        "c": "cd $dir ; taskkill /f /im $fileNameWithoutExt.exe 2>$null ; & 'C:\\Program Files\\mingw64\\bin\\g++.exe' $fileName -o output\\$fileNameWithoutExt.exe -std=c++17 ; & .\\output\\$fileNameWithoutExt.exe"
    }
}
```

### `.vscode/launch.json`
```json
{
    "preLaunchTask": "C/C++: g++.exe build active file",
    "program": "${fileDirname}\\output\\${fileBasenameNoExtension}.exe",
    "miDebuggerPath": "C:\\Program Files\\mingw64\\bin\\gdb.exe"
}
```

---

## 核心经验

| 问题 | 根因 | 方案 |
|------|------|------|
| shell 任务路径异常 | PowerShell 反斜杠 + 引号解析冲突 | 用 `cppbuild` 类型替代 `shell` |
| Code Runner 路径异常 | `$dir` 带尾部 `\` 拼接引号截断 | `cd` 后全部用相对路径 |
| 编译 Permission denied | scanf 导致进程不退出 | 编译前 `taskkill` 杀旧进程 |
| F5 调试编译失败 | launch.json 引用了坏的 shell 任务 | 改为 cppbuild 任务 |

> **一句话总结**：Windows + PowerShell 环境下，尽量避免在引号内拼接含反斜杠的路径变量，优先使用 `cppbuild` 任务类型或相对路径。
